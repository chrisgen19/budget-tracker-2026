/**
 * A copy-and-send reference for what the bot understands.
 *
 * Separate from /help on purpose. /help explains what the bot is and how it behaves; this is a
 * list to copy from, which is what someone actually wants when they cannot remember the phrasing.
 * Every line is a real message that works, not a paraphrase of one.
 */
export const EXAMPLES_MESSAGE =
  `📋 *Things you can send me*\n\n` +
  `*Logging*\n` +
  `\`100 breakfast\`\n` +
  `\`250 jollibee lunch\`\n` +
  `\`+5000 freelance payout\`\n` +
  `\`spent 350 for groceries yesterday\`\n` +
  `_or send a photo of a receipt_\n\n` +
  `*Where the money went*\n` +
  `\`summary\`\n` +
  `\`am I spending more than last month\`\n` +
  `\`show me the last 6 months\`\n` +
  `\`what were my biggest expenses\`\n` +
  `\`where did my work budget go\`\n\n` +
  `*Looking something up*\n` +
  `\`did I pay meralco this month\`\n` +
  `\`how much did I spend at jollibee\`\n` +
  `\`how much on transportation in work budget\`\n` +
  `\`did I pay the water bill\`\n` +
  `\`what did I buy at south supermarket\`\n` +
  `\`what was on that receipt\`\n\n` +
  `*Bills and setup*\n` +
  `\`bills\`\n` +
  `\`recent\`\n` +
  `\`categories\`\n\n` +
  `_Type / to see the same things as buttons. Months work too: try “last month” or “in July”._`;
